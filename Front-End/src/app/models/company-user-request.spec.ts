import { CompanyUserRequest } from './company-user-request';

describe('CompanyUserRequest', () => {
  it('should create an instance', () => {
    expect(new CompanyUserRequest()).toBeTruthy();
  });
});
